// import { EXPORT_QUESTIONS } from '../actions';

const initialState = {
  api: {},
};

const game = (state = initialState, action) => {
  switch (action.type) {
  default:
    return {
      ...state,
    };
  }
};

export default game;
